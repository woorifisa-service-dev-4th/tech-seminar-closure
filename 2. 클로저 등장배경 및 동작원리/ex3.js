const popular_music = "hip-hop";

function body() {
    const popular_music = "Rock & Roll";

    const spirit = function () {
        console.log(popular_music);
    };

    return spirit;
}

const hyunsik = body();
hyunsik();
